approval_policy = "never"
sandbox_mode = "danger-full-access"

[mcp_servers.cilux]
command = "/opt/cilux/bin/cilux-mcp"
args = ["--socket", "/run/cilux-broker.sock"]
default_tools_approval_mode = "approve"
